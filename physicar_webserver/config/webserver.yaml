# PhysiCar WebServer Configuration

webserver:
  host: "0.0.0.0"
  port: 8000
  
  # CORS settings
  cors:
    allow_origins: ["*"]
    allow_methods: ["*"]
    allow_headers: ["*"]
  
  # Rate limiting (TODO)
  rate_limit:
    enabled: false
    requests_per_minute: 60
