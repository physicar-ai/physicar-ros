# Root (/) → 404 (GitHub Codespaces).
# Linked to /tmp/pc-root.conf by entrypoint.sh when CODESPACE_NAME is set.
# VS Code web IS the Codespace itself — no code-server runs in-container,
# so the root is deliberately not served. Legacy /code URLs 404 too.

location / {
    return 404;
}
