
# physicar
export PYTHONPYCACHEPREFIX=/opt/physicar/pycache
export DISPLAY=:1
export ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST
export PHYSICAR_ROS_DIR=/opt/physicar/src/physicar-ros
# Same loopback-pinned CycloneDDS setup as the device — identical
# middleware and transport behavior in SIM and on hardware.
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=file://$PHYSICAR_ROS_DIR/deploy/cyclonedds.xml
export GZ_PARTITION=physicar
export GZ_CONFIG_PATH=/usr/share/gz
export GALLIUM_DRIVER=llvmpipe
export MESA_GL_VERSION_OVERRIDE=3.3
source /opt/ros/jazzy/setup.bash
source ~/physicar_ws/install/setup.bash 2>/dev/null || true
eval "$(register-python-argcomplete ros2)"
eval "$(register-python-argcomplete colcon)"
