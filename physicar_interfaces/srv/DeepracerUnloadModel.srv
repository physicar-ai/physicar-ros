# Unload a DeepRacer model from memory

# Request
string model_name    # Name of model to unload (empty string = unload all)

---

# Response
bool success                    # True if model unloaded successfully
string message                  # Status message or error description
